This chapter shows a multi user chat with a central server.
It shows sending a packet to peer and broadcasting message to all connected peers.

Build & Run:

```sh
odin build client
odin build server
```

```sh
./server
./client
./client
./client
```
